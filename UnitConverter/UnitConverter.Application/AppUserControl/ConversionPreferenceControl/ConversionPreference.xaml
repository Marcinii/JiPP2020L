<UserControl x:Class="UnitConverter.Application.AppUserControl.ConversionPreferenceControl.ConversionPreference"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UnitConverter.Application.AppUserControl.ConversionPreferenceControl"
             mc:Ignorable="d" Height="{Binding height, FallbackValue=50}" d:DesignWidth="300" Background="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Label x:Name="preferenceLabel" Grid.Column="0" Grid.Row="0" HorizontalContentAlignment="Right"
               VerticalContentAlignment="Center" FontSize="16"
               Content="{Binding labelText, FallbackValue=Default value}"
               MouseLeftButtonUp="preferenceLabel_MouseLeftButtonUp"/>

        <ContentPresenter Grid.Column="1" Grid.Row="0"
                          Content="{Binding Path=content, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type UserControl}}}"/>
    </Grid>
</UserControl>
